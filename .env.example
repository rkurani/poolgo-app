# Sign up at https://www.pixellab.ai → Account → API for a token.
# Copy this file to .env.local and paste your real secret there.
# .env.local is gitignored. .env.example is committed as the contract of
# required vars — NEVER put real secrets here.
PIXELLAB_SECRET=your-secret-here
